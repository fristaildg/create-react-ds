const colors = {
  black: '#000000',
  white: '#FFFFFF',
  primary: 'red',
  secondary: 'green',
};

const Button = {
  backgroundColor: colors.primary,
  color: colors.white,
};

const themeDefault = {
  colors,
  Button,
};

export default themeDefault;
